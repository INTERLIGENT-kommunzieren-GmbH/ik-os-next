[Unit]
Description=Watch for the first account to install Homebrew

[Path]
# gnome-initial-setup creates the first account long after multi-user.target.
# Without this, Homebrew is only installed from the second boot onwards.
PathChanged=/etc/passwd
Unit=ik-os-homebrew.service

[Install]
WantedBy=multi-user.target
